apiVersion: v1
kind: Service
metadata:
  name: document-processing-agent
  labels:
    app: document-processing-agent
spec:
  type: NodePort
  selector:
    app: document-processing-agent
  ports:
    - name: http
      port: 8081
      targetPort: 8081
      protocol: TCP